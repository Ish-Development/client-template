{
  "name": "client-kit",
  "private": true,
  "description": "Tools for the research agent. Run npm install once per client folder.",
  "devDependencies": {
    "agent-browser": "0.38.1"
  },
  "scripts": {
    "setup": "npm install && npx agent-browser install"
  }
}
